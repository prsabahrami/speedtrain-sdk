[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "speedtrain"
version = "0.1.0"
description = "SDK for Speedtrain notebooks"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/speedtrain"]
